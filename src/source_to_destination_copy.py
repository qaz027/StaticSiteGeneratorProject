
def copyStaticToDestination(source_dir, dest_dir):
    '''
    Docstring for copyStaticToDestination
    Write a recursive function that copies all the contents from a source directory to a destination directory (in our case, static to public)
        It should first delete all the contents of the destination directory (public) to ensure that the copy is clean.
        It should copy all files and subdirectories, nested files, etc.
        I recommend logging the path of each file you copy, so you can see what's happening as you run and debug your code.
    '''
    # confirm destination is directory
    # if directory - wipe

    # confirm directory is clean

    # confirm source directory is directory
    # copy all files and subdirectories to destination

    #confirm destination directory is same as source



    pass

def wipeDir(directory):
    # confirm directory
    # delete all non folders.
    # if folder, call wipe files on that folder
    # if empty folder delete folder
    pass
